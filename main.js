import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
ReactDOM.render(
	<App />, document.getElementById('app')
	);

//bottom is only uses instead of above if the ReactDOM.render is used on App.js directly
// import App from './App';